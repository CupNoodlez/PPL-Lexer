#ifndef HELPERS_H
#define HELPERS_H

//header for token output
void outputTokens(Token* tokens);

//header for file extension reader
int read_file_ext(const char *filename);

//header for string copy function
void str_copy(char* dest, const char* src);

#endif // HELPERS_H